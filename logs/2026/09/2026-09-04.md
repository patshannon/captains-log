# Captain's Log — 2026-09-04
Pulling together six different workstreams today, which is exactly the kind of day that makes you grateful for structured commit messages—even when the merge train in `private-project-3` threatens to blur everything into one long stream of cycles and pins.

The big story is the product-loop sync landing W18 through W16 and the various W12-W15 merges that finally clear the backlog. The wallet work in W16 is the most substantial piece: the Takes wallet is now fully on the wire, with the catalog exposing an economy block, proper 402 handling for insufficient funds, and lock logic that mirrors the server's authority. The client-side rendering follows suit—the 402 triggers the box office flow, which gates on observe mode (no spending during passive viewing), and the wallet, ledger, and earnings screens all render from live data. This is the kind of integration that touches everything: six dozen files changed for the core half alone, and another 28 for the UI.

The speaker resolver work in W12b is a subtle but important piece—making the client's `resolveSpeakerTag` match the engine's lenient algorithm (exact match → drop leading "THE" → unique whole-word containment) means PRESENTER lines actually find the right face and highlight. The dead SHOT fold is finally gone, which unblocks the camera direction handling that cycle 330 demands.

The Feed work in W17 brings `GET /v1/show/feed` to life with real sharer data, rarity indicators, and view counts. The `recapFramesFromFeed` refactor unifies the recap rendering path across two screens, which should simplify the codebase going forward.

Separately, `private-project-4` and `private-project-5` are wrapping up TEC-1401—the MARC21 cinema notice system that's been percolating for a while. The backend hit production earlier, and today's merges push the frontend handoff live. This is the kind of janitorial work that doesn't get celebrated but makes the platform feel complete for the catalog folks.

The ÉSS registration form fix in `private-project-1` is a small but sensible improvement—deriving the session year from the configured start date rather than hardcoding it means this thing won't break when the calendar flips.

I need to check whether that `eslint: ignore .claude/**` change in the private-project-3 repo is going to play nicely with the other lint configurations we've got. Agent worktrees nested inside the repo is a pattern we're clearly committed to, but the path-scoped XR fence breaking from the root suggests we might want a more robust approach than just silencing the error.

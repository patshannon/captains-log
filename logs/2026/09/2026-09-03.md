# Captain's Log — 2026-09-03
Lot of motion today — 24 commits across a single private repo, but they break into two major arcs: the tail end of the Forge migration (steps 1–11) and the W1–W9 feature ribbon that’s been landing in parallel. The documentation work (ledgers, plans, CLAUDE.md) ties them together, and there’s a small player fix and an auth change that closes out the cycle.

The Forge steps are the big architectural story. Steps 1 and 2 laid the wire (`lib/api/show/forge.ts`) and the source normalizer for the v2 alias table — the $0 heading pre‑normalizer that the API’s segment forge will use. Steps 3–6 built the pure review core, then the hooks for polling, actions, and publish, and finally the `/forge/[forgeId]` review screen itself. Steps 7 and 8 rerouted the segment forge through the engine and moved the segment export to an API GET, deleting the old client‑side `segmentExport` module. Step 9 added a reference‑image intake on the visual style side (one flash call to extract style notes). Step 10 deleted the Phase 0.5 bridge — all those `src/app/api/` routes, the `forgeBridge`, `forgeOutput`, `showSheet`, `showLint`, `useShowForge` — a satisfying cleanup of 24 files gone. Step 11 was pure docs: updated CLAUDE.md to reflect the new surface, the architecture map, and the product direction. The whole migration replaces an in‑repo API shim with direct calls to the real API, and I feel better about the remove‑and‑replace pattern here — the bridge was useful for speed but had grown a crust of dead branches.

Alongside that, W1–W9 work has been shipping through API cycles 311–319. W1/W2 gave us the run events channel (SSE reader over fetch with Zod frames) and the lookahead landing — crucial for real‑time player state. W7 cleaned up the cold open shape (no more separate `ShowIntroCaptionLineSchema`, just `ShowBeatLine` with a sentinel). W5 added reference images in the editors (signed thumbnails, vision helpers, a “Describe from image” flow that pre‑fills layout and lighting). W8 pushed cast hooks and segment mission data onto the public projection. W9 replaced the hardcoded GEMINI_VOICES list with a live voice catalog from the TTS API, complete with sample playback in the character editor. The auth change finally lets any signed‑in account play, with writer gated to cms admin/creator or internal — a sensible split. And I had to add a temporary strip of SHOT/CAMERA rows from the cold open in the player, pending API ask A14. The docs ledger (API_SYNC) now has all work weeks tracked with commit hashes, which will make audits easier.

Heavy day, but it feels like a plateau — the Forge migration is fully landed, the feature work is caught up through API cycle 322, and the documentation is coherent again. Tomorrow I should focus on the open API asks (A14, A17, A18) and maybe start cleaning up the tech debt on the player side.

# Captain's Log — 2026-08-13
Heavy day today. Twenty-one commits across five projects, plus a PR merge that’s been cooking for a while. Let me walk through what I actually did.

**private-project-1** got a big branding push. I swapped the login header text for a proper TQ logo, added favicons, tightened up the page title, and slapped a `noindex` on everything—this is an internal tool, no reason to let it leak into search results. I also pinned all timestamps to Montreal time with an explicit timezone label. That’s one of those decisions that feels pedantic until you’re debugging a sync that ran at “2:00” and you can’t tell if it’s UTC or local. Now it’s unambiguous. The fire-and-forget publish UI was the trickiest part: when the user hits publish, we kick off the deploy and immediately show a toast, but some syncs get skipped (no new data). I added a toast for that case too, so the user isn’t left wondering. Finally, I wired up the production deploy target – separate from staging, with its own DNS and certificate. That involved a few config files and a sanity check on the pipeline.

**private-project-2** is the cinema feature that finally landed. I merged PR #21, which had been sitting in review for a while. The core

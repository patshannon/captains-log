# Captain's Log — 2026-08-15
A quiet day, but the commits tell a story of cleaning up loose ends and adding substance. The bulk of the work landed in `private-project-1`, where I tackled three distinct threads.

First, the PublishHistory table had a nagging visual misalignment — the "Détails" header wasn't centered. A one-line CSS fix, but it's the kind of thing that catches the eye every time you look at the page. Satisfying to close that.

Second, I disabled Firebase Hosting caching (`no-store`) on both staging and production targets. We've been fighting stale responses when deploying new features, and the default caching behavior was causing confusion during demos. This is a blunt instrument — I'd rather have a proper cache-busting strategy — but it buys us confidence until we can design something smarter. The tradeoff is slightly higher load on the origin, but for our traffic levels it's negligible.

The largest commit was a feature: honest publish progress, history content breakdown, and a `?mock` demo mode. The "honest" part means the progress indicator now actually reflects real server-side state rather than optimistic local estimates. The history content breakdown adds a per-section tally of what's been published versus what's pending — something the team has been asking for. And the mock mode lets me demo the full flow without touching real data, which is essential for both development and stakeholder reviews. It's a simple query parameter that swaps in a fixture dataset, but wiring it through the existing state management required careful isolation so it doesn't leak into production paths.

I also wrote a documentation handoff for yesterday's pipeline incident — root cause, the fixes applied, and a runbook entry for the future. Boring but critical work. The two pushes to `main` (one for the private project, one for `captains-log`) were just syncing — no new commits, which means I likely merged a PR or rebased.

Not a heavy day, but I feel good about the quality. The mock mode alone will save me hours of setup time going forward.

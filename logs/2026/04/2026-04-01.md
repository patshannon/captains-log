# Captain's Log — 2026-04-01
Busy day across three fronts, with a healthy mix of feature work, bug fixes, and some housekeeping I've been meaning to get to.

The most substantial work happened in private-project-3. Merged the 404 page implementation — 60 files changed, which sounds alarming but was largely a develop branch merge bringing in accumulated work alongside the feature itself. The reusable tabs feature is also moving forward; I opened a fresh PR after closing the previous one, which usually means a significant enough rework that a clean history was worth it. The review feedback there was concrete and reasonable: extract a `useIsDesktop` hook rather than inlining the breakpoint logic, and clean up test conventions. I also caught a `className` override ordering bug in the button component — the kind of subtle CSS specificity/ordering issue that's easy to miss in review but shows up embarrassingly in production. Good that it got caught now.

On the private-project-1 and private-project-2 side, the day was mostly about sync and recovery work. Restored a `createContribution` option that had gone missing from the partner navigation — these kinds of regressions are frustrating because they're often invisible until a specific user hits that flow. Restoring it touched minimal files, which suggests it was a config or conditional render issue rather than anything architectural. Also ran the usual prod-to-staging and prod-to-dev sync PRs to keep branches aligned, and merged a production sync on the other project.

One thing I did across both private-project-1 and private-project-3 was restructure the AI agent documentation. The pattern I landed on: `AGENTS.md` holds the actual content, `CLAUDE.md` just points to it. The motivation is token efficiency — Claude reads `CLAUDE.md` by convention, but bloating that file with everything means burning context budget on boilerplate. Indirecting to a leaner `AGENTS.md` keeps the working context tight. Small thing, but the kind of hygiene that compounds over time when you're working closely with LLM tooling.

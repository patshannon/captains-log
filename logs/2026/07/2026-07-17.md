# Captain's Log — 2026-07-17
Two features landed today, both in the same private project, and both feeling like meaningful steps toward a more polished product.

The bigger one conceptually: profile pages now live at `/@username` instead of `/profile`. This is the TikTok/Twitter convention — short, clean, and immediately recognizable to users. Ten files changed to make it happen, which suggests it wasn't just a route rename. You have to think through redirects, link generation across the app, any hardcoded paths, and whatever the router is doing under the hood. PR #21 covers the full surface area — own profile, public-facing profile, edit flow, and avatars — so this was a real feature push, not a cosmetic refactor. The `/@username` pattern also sets you up nicely for social graph features down the road; it signals intent about what the app is.

The second commit cut the community invites flow over to the live backend, ending what I assume was a stretch of running against mocks or a staging environment. "Cycle 089" in the message tells me this project tracks work in explicit development cycles, which I find useful — it creates natural checkpoints and makes it easier to reason about what shipped when. Eleven files changed for a backend cutover usually means updating service calls, handling real error states, adjusting auth flows, and probably ripping out whatever fake data was propping things up before. The fact that it landed cleanly is the whole point.

Light day in terms of volume, but both commits represent the kind of work that moves a product forward rather than just maintaining it — routing conventions that feel intentional, and infrastructure that's now real.

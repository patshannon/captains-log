# Captain's Log — 2026-06-05
Heavy day — three active projects, two feature PRs opened and merged, and a lot of logic fixes that required careful thinking about edge cases in domain-heavy code.

The bulk of the work was split between two private projects that share thematic concerns: caseload management, follow-up scheduling, and field synchronization in what looks like a case management or social services context. The French domain vocabulary — "suivi à brève échéance," "Placé PH1/PH2," "maintien Actif" — signals this is specialized software where getting the business logic precisely right matters more than almost anything else.

On private-project-2, the main arc was getting short-term follow-ups ("suivi à brève échéance") properly included in the Placé PH1 caseload view and export. The underlying bug was an exclusion/inclusion problem: scheduled follow-ups weren't being counted, and closed files were incorrectly leaking into the "maintien Actif" branch. The caseload queries also had deduplication issues that needed cleaning up — classic symptoms of queries that grew organically over time and accumulated redundancy. Also had to declare some missing test dependencies in `package.json`, which is the kind of small-but-blocking oversight that only surfaces when you actually try to run the test suite somewhere clean.

Private-project-3 had more surface area. The big admission form fix was guarding the "suivi à brève échéance" field so it only activates on the correct status values (2 and 3), and moving date computations over to moment.js for consistency. Date logic in case management systems is notoriously fiddly — moment gives you a reliable foundation rather than hand-rolling arithmetic that silently breaks on month boundaries. The caseload fix here mirrored private-project-2: keying the Placé PH1 suivi type and date on a pending short-term follow-up rather than whatever was previously driving it. The large merge (116 files) folding dev into master suggests this has been accumulating for a while.

Both projects also got AGENTS.md consolidated as the single source of truth for agent guidance — something I've been standardizing across repos so AI-assisted workflows have a consistent, authoritative place to look for project-specific context.

Private-project-1 was just a submodule pointer bump. Routine, but it means the dependent projects are tracking updated frontend and backend refs.
